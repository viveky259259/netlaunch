import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color darkNavy = Color(0xFF111827);
  static const Color teal = Color(0xFF10B981);

  // Backgrounds
  static const Color lightGrayBg = Color(0xFFF9FAFB);
  static const Color white = Color(0xFFFFFFFF);

  // Borders & Text
  static const Color cardBorder = Color(0xFFE5E7EB);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textPrimary = Color(0xFF111827);

  // Hero gradient
  static const Color heroGradientStart = Color(0xFF0A0A0A);
  static const Color heroGradientEnd = Color(0xFF1A1A2E);

  // Status colors
  static const Color statusLive = Color(0xFF10B981);
  static const Color statusBuilding = Color(0xFFF59E0B);
  static const Color statusFailed = Color(0xFFEF4444);

  // Misc
  static const Color tealLight = Color(0xFFD1FAE5);
  static const Color navyLight = Color(0xFF1F2937);
}
