import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color darkNavy = Color(0xFF111827);
  static const Color teal = Color(0xFF10B981);
  static const Color tealDark = Color(0xFF059669);

  // Dark backgrounds (matching landing page)
  static const Color bg = Color(0xFF0A0A12);
  static const Color bgCard = Color(0xFF0F1018);
  static const Color cardSurface = Color(0xFF13131A);

  // Legacy light (kept for compatibility)
  static const Color lightGrayBg = Color(0xFF0A0A12);
  static const Color white = Color(0xFF13131A);

  // Borders & Text
  static const Color cardBorder = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textPrimary = Color(0xFFE2E8F0);

  // Hero gradient
  static const Color heroGradientStart = Color(0xFF0A0A12);
  static const Color heroGradientEnd = Color(0xFF111827);

  // Status colors
  static const Color statusLive = Color(0xFF10B981);
  static const Color statusBuilding = Color(0xFFF59E0B);
  static const Color statusFailed = Color(0xFFEF4444);

  // Misc
  static const Color tealLight = Color(0xFF064E3B);
  static const Color tealGlow = Color(0x1F10B981);
  static const Color navyLight = Color(0xFF1F2937);
  static const Color dimText = Color(0xFF475569);
}
