name: netlaunch_core
description: Core models, validators, and constants for NetLaunch. Pure Dart — no Flutter or Firebase dependency.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0
